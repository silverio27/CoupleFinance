export interface Category {
    id: number;
    color: string;
    description: string;
    icon: string;
  }
  export interface NewCategory {
    color: string;
    description: string;
    icon: string;
  }